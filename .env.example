# Required: your Vercel AI Gateway API key (used for every image generation).
# Create one at https://vercel.com/dashboard → AI Gateway → API Keys.
AI_GATEWAY_API_KEY=

# Required: Vercel Blob read/write token (generated images are stored here).
# Added automatically when you attach Blob storage to your Vercel project.
BLOB_READ_WRITE_TOKEN=
